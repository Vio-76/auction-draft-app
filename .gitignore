# dependencies
node_modules/

# database (runtime state — never commit live auction data)
auction.db
auction.db-journal
auction.db-wal
auction.db-shm
*.db

# uploaded player images (runtime; on Render these live on the /data disk instead)
uploads/

# secrets / local config
.env
.env.*
!.env.example

# Claude working notes (kept out of the public repo, like the sibling project)
CLAUDE.md

# logs
*.log
npm-debug.log*

# editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
